import React from "react"; // Import React to use JSX

function App() { // Define the App component
  return ( 
    <h1>Hello, World!</h1> // Display Hello, World! on the page
  );
}

export default App; // Export the function so other files can use it
